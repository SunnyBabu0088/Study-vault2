{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome (StudyVault)",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:5500",
            "webRoot": "${workspaceFolder}/frontend"
        }
    ]
}